name:  💿 Sync iStoreOS Imagebuilder

on:
  schedule:
    - cron: '0 19 * * 2' # 每周三北京时间凌晨 03:00 自动运行
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Parse Remote Date and Check Local
        id: parse
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          BASE_URL="https://fw.koolcenter.com/iStoreOS/ib/x86_64/"
          TARGET_FILE="istoreos-imagebuilder-x86-64.Linux-x86_64.tar.zst"
          
          # 1. 获取包含文件名的行
          LINE_CONTENT=$(curl -sL $BASE_URL | grep "$TARGET_FILE" | head -n 1)
          
          # 2. 提取日期 (YYYY-MM-DD)
          RAW_DATE=$(echo "$LINE_CONTENT" | grep -oE '[0-9]{4}-[0-9]{2}-[0-9]{2}' || echo "")
          
          if [ -z "$RAW_DATE" ]; then
            echo "::error::无法在页面行内识别日期格式"
            exit 1
          fi

          # 3. 格式化并生成目标文件名
          RELEASE_DATE=$(date -d "$RAW_DATE" +%Y%m%d)
          FILENAME="${RELEASE_DATE}-${TARGET_FILE}"
          
          echo "date=$RELEASE_DATE" >> $GITHUB_OUTPUT
          echo "filename=$FILENAME" >> $GITHUB_OUTPUT
          echo "url=${BASE_URL}${TARGET_FILE}" >> $GITHUB_OUTPUT

          # 4. 检查是否已存在
          EXISTS=$(gh release view iStoreOS-Imagebuilder --json assets --jq ".assets[].name" 2>/dev/null | grep -w "$FILENAME" || true)

          if [ "$EXISTS" = "$FILENAME" ]; then
            echo "skip_upload=true" >> $GITHUB_OUTPUT
            echo "✅ 文件 $FILENAME 已存在，无需重复同步。"
          else
            echo "skip_upload=false" >> $GITHUB_OUTPUT
            echo "🚀 发现新版本或缺失版本: $FILENAME"
          fi

      - name: Download File
        if: steps.parse.outputs.skip_upload == 'false'
        run: |
          echo "正在从官网拉取: ${{ steps.parse.outputs.filename }}"
          # 使用 -C - 开启断点续传，防止大文件下载中断
          curl -L -C - "${{ steps.parse.outputs.url }}" -o "${{ steps.parse.outputs.filename }}"

      - name: Upload to Release
        if: steps.parse.outputs.skip_upload == 'false'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: iStoreOS-Imagebuilder
          files: ${{ steps.parse.outputs.filename }}
          body: |
            ## iStoreOS Imagebuilder 自动同步
            - **最新源文件更新日期**: ${{ steps.parse.outputs.date }}
            - **官方地址**: [Koolcenter](https://fw.koolcenter.com/iStoreOS/ib/x86_64/)
          make_latest: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
